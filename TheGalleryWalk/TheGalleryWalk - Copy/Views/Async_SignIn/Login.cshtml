@model TheGalleryWalk.Models.LoginDataPrecheck

@{
    ViewBag.Title = "Login Username";
}

<div class="GalleryContent" style="padding-top:50px;">
   
      @using (Html.BeginForm("Login", "Async_SignIn", FormMethod.Post, new { id = "SignUpForm" }))
      {
        <fieldset class="RegisterForm">
            <h3 style="margin:auto;padding:15px; padding-top:15px; padding-left:15px; color:white;">
                Login
            </h3>

            <ol class="formField">
                @Html.LabelFor(model => model.EmailAddress, new { @class = "formFieldTitle" })
                @Html.TextBoxFor(model => model.EmailAddress, String.Empty, new { @class = "formFieldInput" })
                @Html.ValidationMessageFor(model => model.EmailAddress, String.Empty, new { @class = "errorMessage" })
            </ol>
          
            <div id="submitButton" style="margin:auto; width:67px; margin-top:20px; color:white; margin-bottom:20px; text-align:center; border: 2px solid white; border-radius:5px;">
                <button id="buttonText" name="submitForm" style="background-color:transparent; border:none; border-radius:5px; color:white;">Next</button>
            </div>

        </fieldset>


      }

    <h3>Need an account? Register as </h3>
    <div style="text-align:center; margin-top:50px;">
        <a class="menuItem portalOptionWindow" style="color:black;" href="@Url.Action("Signup","Async_SignUp")"><span class="glyphicon glyphicon-user"></span> Gallery Owner</a>
        <a class="menuItem portalOptionWindow" style="color:black;" href="@Url.Action("Signup" , "Artist_Async_SignUp")"><span class="glyphicon glyphicon-log-in"></span> Artist</a>
    </div>

        <script type="text/javascript">

            $(document).ready(function () {

                $('#submitButton').mouseenter(function () {
                    $(this).css({ "background-color": "white" });
                    $(this).find('#buttonText').css({ "color": "deepskyblue" });
                }).mouseleave(function () {
                    $(this).css({ "background-color": "deepskyblue" });
                    $(this).find('#buttonText').css({ "color": "white" });

                });
            });

        </script>
    </div>