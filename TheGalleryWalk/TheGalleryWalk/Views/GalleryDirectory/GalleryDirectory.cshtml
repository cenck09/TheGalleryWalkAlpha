@using System.Diagnostics;
@using TheGalleryWalk.Models;
@using Parse;
@model IEnumerable<GalleryEntity>
@{
    ViewBag.Title = "GalleryDirectory";
    // Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>GalleryDirectory</h2>


<div style="text-align:center;">
    @try
    {
        Html.RenderPartial("~/Views/GalleryDirectory/GalleryPartialView.cshtml", Model);
    }
    catch (Exception ex)
    {
        Debug.WriteLine("There was an error ::    " + ex);
    }
</div>

