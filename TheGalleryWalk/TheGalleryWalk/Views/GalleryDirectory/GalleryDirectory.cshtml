@using System.Diagnostics;
@using TheGalleryWalk.Models;
@using Parse;
@model IEnumerable<GalleryEntity>
@{
    ViewBag.Title = "ArtistUserDirectory";
    // Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>GalleryDirectory</h2>


<div style="text-align:center;">
    @try
    {
        Html.RenderPartial("~/Views/ArtistUserDirectory/GalleryPartialView.cshtml", Model);
    }
    catch (Exception ex)
    {
        Debug.WriteLine("There was an error ::    " + ex);
    }
</div>

