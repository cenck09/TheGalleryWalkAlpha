@using System.Diagnostics;
@using TheGalleryWalk.Models;
@using Parse;
@model TheGalleryWalk.Models.ArtistUserDirectoryManager
@{  //model IEnumerable<ArtistUserEntity>
    ViewBag.Title = "ArtistUserDirectory";
    // Layout = "~/Views/Shared/_Layout.cshtml";
}


<div style="text-align:center;">
    @try
    { Html.RenderPartial("~/Views/PageManagerView/PageManagerNumberView.cshtml", Model.PageManager); }
    catch (Exception ex)
    { Debug.WriteLine("There was an error ::    " + ex); }

    @try
    {
        Html.RenderPartial("~/Views/ArtistUserDirectory/ArtistUserPartialView.cshtml", Model.artistUserEntities);
    }
    catch (Exception ex)
    {
        Debug.WriteLine("There was an error ::    " + ex);
    }

    @try
    { Html.RenderPartial("~/Views/PageManagerView/PageManagerNumberView.cshtml", Model.PageManager); }
    catch (Exception ex)
    { Debug.WriteLine("There was an error ::    " + ex); }
</div>

